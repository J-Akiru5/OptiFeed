// TODO(karl): Build the feeding history list (past feeding events, volumes, statuses, filters).
export default function HistoryPage() {
	return (
		<div>
			<h1 className="text-3xl font-bold text-[var(--ofd-base)]">History</h1>
			<p className="mt-2 text-gray-700">Review past feeding events and dispensed volumes.</p>
		</div>
	);
}
