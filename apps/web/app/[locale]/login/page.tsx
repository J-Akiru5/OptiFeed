// TODO(karl): Build the Supabase Auth login form (email/password, magic link, OAuth if required).
export default function LoginPage() {
	return (
		<div className="flex flex-1 flex-col items-center justify-center p-6">
			<h1 className="text-3xl font-bold text-[var(--ofd-base)]">Log In</h1>
			<p className="mt-2 text-gray-700">Supabase Auth login form goes here.</p>
		</div>
	);
}
