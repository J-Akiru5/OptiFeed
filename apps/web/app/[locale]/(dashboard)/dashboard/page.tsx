// TODO(karl): Build the dashboard home with glanceable pond metrics, device status, and quick actions.
export default function DashboardHomePage() {
	return (
		<div>
			<h1 className="text-3xl font-bold text-[var(--ofd-base)]">Dashboard</h1>
			<p className="mt-2 text-gray-700">Home overview for ponds, devices, and feed status.</p>
		</div>
	);
}
