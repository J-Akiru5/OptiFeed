// TODO(karl): Build the notifications list (tiers, read/unread, filtering, deep links).
export default function NotificationsPage() {
	return (
		<div>
			<h1 className="text-3xl font-bold text-[var(--ofd-base)]">Notifications</h1>
			<p className="mt-2 text-gray-700">View and manage pond alerts and system messages.</p>
		</div>
	);
}
