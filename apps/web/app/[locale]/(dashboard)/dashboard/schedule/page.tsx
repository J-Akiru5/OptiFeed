// TODO(karl): Build the feeding schedule editor (feeds per day, start/end times, rate percentage).
export default function SchedulePage() {
	return (
		<div>
			<h1 className="text-3xl font-bold text-[var(--ofd-base)]">Schedule</h1>
			<p className="mt-2 text-gray-700">Configure daily feeding schedules.</p>
		</div>
	);
}
