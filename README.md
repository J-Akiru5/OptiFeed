
  # Create project plan

  This is a code bundle for Create project plan. The original project is available at https://www.figma.com/design/GSd0c0ZAp2tPGy7rCDhLDD/Create-project-plan.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # OptiFeed
